{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "SKILL=$(jq -r '.tool_input.skill // .tool_input.args' 2>/dev/null); if [ \"$SKILL\" = \"commit-commands:commit\" ] || [ \"$SKILL\" = \"commit-commands:commit-push-pr\" ]; then cd /Users/arend/Developer/flemish-network && npm run lint 2>&1 && npm run typecheck 2>&1; else exit 0; fi",
            "timeout": 60,
            "statusMessage": "Running lint + typecheck before commit..."
          }
        ]
      }
    ]
  }
}
