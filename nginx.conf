server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Support React Router direct URL loads by falling back to index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}